implement signals
code exit
implement subshells






///////////redirections 
cmd | cmd | cmd

cmd : suivie d 1 autre ?
	si oui on ecrit dans un pipe sauf si une redir est precisee
apres exec, 
	on lit depuis le pipe sauf si redirection 
