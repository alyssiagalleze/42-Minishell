

echo a -> command
echo a | echo b -> pipeline
echo a | echo b -> list
(echo a | echo b) -> list in a subshell
/!\ les choses serieuses commencent :
echo a -> one list of one pipeline composed of one command
echo a | echo b && echo c | echo d -> two lists, each composed of one pipeline composed of two commands
((echo a) && (echo b)) || echo c
echo a || ((echo b) && (echo c)) || echo d || echo e
echo a || echo b || echo c || echo d && echo e
echo a && echo b || echo c || echo d && echo e

false && (true || echo a)
false && true || echo a

BASH WORKFLOW:

-> man workflow

	1. Reads its input from a file, from a string supplied as an argument to the -c invocation option, or from the user’s terminal.
	2. Breaks the input into words and operators, obeying the quoting rules. These tokens are separated by metacharacters. This step performs alias expansion.
	3. Parses the tokens into simple and compound commands.
	4. Performs the various shell expansions, breaking the expanded tokens into lists of filenames and commands and arguments.
	5. Performs any necessary redirections and removes the redirection operators and their operands from the argument list.
	6. Executes the command.
	7. Waits for the command to complete and collects its exit status.

-> explanation
	1.	from a file : script shell // i don't think we care
		from a string supplied as an argument to the -c invocation option : "bash -c ls" -> execute ls but terminal not set in bash // i don't think we care

	2.	words : a sequence of characters treated as a unit by the shell, without unquoted metacharacters
		operator : a sequence of metacharacters treated as a unit by the shell.
			control operator : performs a control function '()', '|', '||', '&&', etc -> order of operation etc
			redirection operator : >, <, >>, << -> in and out
		quoting rules : single quote / double quotes / ANSI-C quoting ($'\t', \n etc) i think we don't care / Locale-Specific Translation ($"text" // i think we don't care)
		tokens : a word or an operator
		metacharacters : characters that separate words -> space / tab / newline / ( / ) / | / & / > / <
		alias expansion : alias as www for werror wall wextra // i think we don't care since it needs built-in function we don't have to make

	3.	simple command : sequence of words separated by blanks, terminated by a control operator
			simple commands can be organized in different constructs:
				-> pipeline : echo a | echo b
				-> list

	4.	shell expansions : 
			(+ means it can increase the words number)
			+ brace expansion = a{b, c, d} -> ab ac ad // we don't care
			- tilde expansion = ~login name // we don't care
			- $parameter expansion = 
			- $variable expansion = 
			- $arithmetic expansion = $((2 * 3)) // we don't care
			- $command substitution = $(echo ls) -> ls , the command result becomes a command itself // we don't care
			//
			+ word splitting = -> recupere les expansions et split en se basant sur white space (complexe)
			//
			+ filename expansion -> scan for char * ? [ and make it a pattern
				-> we only care for * :
					Matches any string, including the null string. When the globstar shell option
					is enabled, and ‘*’ is used in a filename expansion context, two adjacent ‘*’s used
					as a single pattern match all files and zero or more directories and subdirectories.
					If followed by a ‘/’, two adjacent ‘*’s match only directories and subdirectories.
				-> subject : in the current directory
			//
			- quote removal = -> remove every ' " | that is unquoted / did not result from previous expansion


-> simplified workflow

	1. Reads its input from the user’s terminal.
	2. Separate the input into words or operators, obeying "", ''
	2.5. Check for syntaxe error ?
	3. Parses the tokens into simple and compound commands.
	3.5 From this point, I suspect Bash treats each list of commands individualy
	4. Performs the various shell expansions, breaking the expanded tokens into lists of filenames and commands and arguments.
	5. Performs any necessary redirections and removes the redirection operators and their operands from the argument list.
	6. Executes the command.
	7. Waits for the command to complete and collects its exit status.

6. command execution workflow

	3.7.1 Simple Command Expansioninclude
	When the shell executes a simple command, it performs the following expansions, assign-
	ments, and redirections, from left to right, in the following order.
	1. The words that the parser has marked as variable assignments (those preceding the
	command name) and redirections are saved for later processing.
	2. The words that are not variable assignments or redirections are expanded (see
	Section 3.5 [Shell Expansions], page 24). If any words remain after expansion, the
	first word is taken to be the name of the command and the remaining words are the
	arguments.
	3. Redirections are performed as described above (see Section 3.6 [Redirections], page 41).
	4. The text after the ‘=’ in each variable assignment undergoes tilde expansion, parameter
	expansion, command substitution, arithmetic expansion, and quote removal before
	being assigned to the variable
